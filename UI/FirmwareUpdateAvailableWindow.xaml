<Window x:Class="SC3RGBController.UI.FirmwareUpdateAvailableWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Mixer Firmware Update Available" Width="510" Height="315" ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner" Background="#090909" Foreground="White" ShowInTaskbar="False">
    <Border BorderBrush="#343434" BorderThickness="1" CornerRadius="14" Padding="26" Background="#0D0D0D">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/><RowDefinition Height="8"/><RowDefinition Height="Auto"/>
                <RowDefinition Height="22"/><RowDefinition Height="Auto"/><RowDefinition Height="22"/><RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <TextBlock Text="Mixer Firmware Update Available" FontSize="21" FontWeight="SemiBold"/>
            <TextBlock Grid.Row="2" Text="Firmware 1.5 adds support for Custom Button Shortcuts."
                       Foreground="#B7B7B7" FontSize="12" TextWrapping="Wrap"/>
            <Border Grid.Row="4" Background="#121212" BorderBrush="#303030" BorderThickness="1" CornerRadius="10" Padding="16">
                <Grid>
                    <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
                    <StackPanel>
                        <TextBlock Text="Current" Foreground="#858585" FontSize="11"/>
                        <TextBlock Text="RGB+ Firmware 1.4" FontSize="14" FontWeight="SemiBold" Margin="0,4,0,0"/>
                    </StackPanel>
                    <StackPanel Grid.Column="1">
                        <TextBlock Text="Latest" Foreground="#858585" FontSize="11"/>
                        <TextBlock Text="RGB+ Firmware 1.5" FontSize="14" FontWeight="SemiBold" Margin="0,4,0,0"/>
                    </StackPanel>
                </Grid>
            </Border>
            <StackPanel Grid.Row="6" Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="Later" Width="95" Height="36" Margin="0,0,10,0" Click="LaterButton_Click"/>
                <Button Content="Update Firmware" Width="150" Height="36" Background="#151B17" BorderBrush="#4AA45B" Foreground="White" Click="UpdateButton_Click"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>